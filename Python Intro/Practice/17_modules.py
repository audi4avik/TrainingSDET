# Modules are used to organize same set of classes & functions together
import math_functions   # importing the whole module = all functions or classes
from math_functions import division     # importing specific method or class

print(math_functions.addition(10, 45))
print(division(23, 2))